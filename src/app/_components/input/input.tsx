import {
  Dialog,
  DialogContent,
  DialogTrigger,
} from "@/components/ui/dialog";

const AddFiles = () => {
  return (
    <Dialog>
      <DialogTrigger>Add Files</DialogTrigger>
      <DialogContent>
       form goes here
      </DialogContent>
    </Dialog>
  );
};

export default AddFiles;
