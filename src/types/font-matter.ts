export interface FrontMatter {
  title: string;
  headline: string;
  description: string;
  button: string;
  buttonLink: string;
  [key: string]: string;
}

export interface Feature {
  title: string;
  description: string;
}
